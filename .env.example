# MemoBrain Configuration Template
# Copy this file to .env and fill in your values

# z.ai API Configuration (Anthropic-compatible endpoint)
ZAI_API_KEY=your_api_key_here
ZAI_BASE_URL=https://api.z.ai/api/anthropic

# Available Models:
# - GLM-4.5-Air : Fast, cheap (recommended for testing)
# - GLM-4.7     : Best quality (recommended for production)
ZAI_MODEL=GLM-4.5-Air

# Memory Storage Path
# Path where memory sessions are persisted as JSON files
# Can be relative (to memobrain/ directory) or absolute
# Default: ./_bmad-output/memory
#
# Examples:
#   MEMOBRAIN_STORAGE_PATH=../_bmad-output/memory   # Parent project's output folder
#   MEMOBRAIN_STORAGE_PATH=/var/data/memobrain      # Absolute path
#   MEMOBRAIN_STORAGE_PATH=./data/memory            # Local to memobrain folder
MEMOBRAIN_STORAGE_PATH=../_bmad-output/memory
